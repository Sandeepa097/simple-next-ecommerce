const { resolve } = require('path');

module.exports = {
  config: resolve('server/config/database.mjs'),
  'seeders-path': resolve('server/seeders'),
  'migrations-path': resolve('server/migrations'),
  'models-path': resolve('dist/models'),
};
